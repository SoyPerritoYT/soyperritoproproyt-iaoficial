export default async function handler(req,res){
 const raw=req.headers.cookie?.match(/(?:^|; )github_token=([^;]*)/)?.[1]; if(!raw)return res.status(200).json({connected:false});
 try{const token=decodeURIComponent(raw),gh=await fetch("https://api.github.com/user",{headers:{Accept:"application/vnd.github+json",Authorization:"Bearer "+token,"User-Agent":"SoyPerritoProProYT-IAOFICIAL"}});if(!gh.ok)return res.status(200).json({connected:false});const user=await gh.json();return res.status(200).json({connected:true,login:user.login,name:user.name||"",avatar_url:user.avatar_url||""});}catch{return res.status(200).json({connected:false});}
}